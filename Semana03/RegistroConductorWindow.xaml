<Window x:Class="Semana03.RegistroConductorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registro de Conductores" Height="260" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="REGISTRO DE CONDUCTOR" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- a. Nombre Conductor -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Nombre Conductor:" Width="130" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombre" Width="210" Height="25" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- b. Licencia -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Licencia:" Width="130" VerticalAlignment="Center"/>
            <TextBox x:Name="txtLicencia" Width="210" Height="25" VerticalContentAlignment="Center"/>
        </StackPanel>

        <!-- c. Transporte -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Transporte:" Width="130" VerticalAlignment="Center"/>
            <TextBox x:Name="txtTransporte" Width="210" Height="25" VerticalContentAlignment="Center"/>
        </StackPanel>

        <Button Content="Registrar Conductor" Grid.Row="4" Height="32" Margin="0,15,0,0" Click="btnRegistrar_Click" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
    </Grid>
</Window>